#!/usr/bin/env Rscript

library("sdmengine")
start_engine("./workshop")

# library("sdmengine.dismo")
# result <- query_progress_from_database("./workshop")
#
# result_list <- split(result, rownames(result))
